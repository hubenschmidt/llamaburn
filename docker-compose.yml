services:
  cli:
    build: .
    stdin_open: true
    tty: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./results:/app/results
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
